/**
 * Token Aggregator
 *
 * Re-export from shared for backward compatibility.
 * New code should import directly from @shared/utils/tokenAggregator
 */

export { aggregateTokens, type TokenUsage } from '@shared/utils/tokenAggregator';
