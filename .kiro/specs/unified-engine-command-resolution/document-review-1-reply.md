# Response to Document Review #1

**Feature**: unified-engine-command-resolution
**Review Date**: 2026-02-03
**Reply Date**: 2026-02-03

---

## Response Summary

| Severity | Issues | Fix Required | No Fix Needed | Needs Discussion |
| -------- | ------ | ------------ | ------------- | ---------------- |
| Critical | 0      | 0            | 0             | 0                |
| Warning  | 2      | 0            | 2             | 0                |
| Info     | 3      | 0            | 3             | 0                |

---

## Response to Warnings

### W-1: IPCレイヤー統合テストが明示的でない

**Issue**: Task 7.3の範囲を明確化し、IPC経由の`startAgent`呼び出しも含めることを推奨

**Judgment**: **No Fix Needed** ❌

**Evidence**:

1. **design.md (lines 302-315)** で Integration Test Strategy が明確に定義されている:
   ```
   startAgent({engineId: 'claude'})
     -> EngineCommandResolverService.resolveCommand('claude')
     -> ClaudePathResolverService.getClaudePath()
     -> spawn(resolvedCommand, args)
   ```

2. **Task 7.3** は `SpecManagerService.startAgent` でのコマンド解決を検証する。これはビジネスロジックの核心であり、IPCハンドラーは単なる薄いラッパー（パラメータを渡すだけ）

3. **design.md (lines 317-319)** で Mock Boundaries が定義済み:
   - `ClaudePathResolverService`: 実際のシェル呼び出しをモック
   - `AgentProcess.spawn`: プロセス起動をモック

4. IPCハンドラーを通したテストは、追加のカバレッジをほぼ提供しない（ハンドラーは引数をサービスに転送するのみ）。サービス層のテストで十分。

5. E2Eテスト（Task 9.2）が全体のエンドツーエンドフローを検証する

**Reason**: テスト戦略は design.md で十分に定義されており、IPCハンドラーのテストは冗長。サービス層でのユニット/統合テストと E2E テストの組み合わせで適切なカバレッジを確保。

---

### W-2: Remote UI側`engineId`パラメータハンドリング

**Issue**: `remote-ui/`側のAPIクライアント更新がTask 4.4でカバーされることを確認。実装時にRemote UI E2Eテストも考慮

**Judgment**: **No Fix Needed** ❌

**Evidence**:

1. **Task 4.4** で `WebSocketApiClient` の更新が明記:
   > `command`パラメータを`engineId`に変更
   > WebSocket経由のリクエストで`engineId`を送信

2. **Task 6.1** で `webSocketHandler.ts` の更新が明記:
   > WebSocket経由のリクエストで`engineId`を受け取るよう変更
   > 内部で`startAgent({engineId})`を呼び出し

3. **design.md (lines 221-222)** の Summary-Only Components で両方のファイルが明記:
   - `WebSocketApiClient.ts`: `command`を`engineId`に
   - Impact Analysis Contract (line 296) にも記載

4. **Requirements Traceability (lines 124-125)** で Remote UI 対応がマッピング済み:
   - Criterion 7.1-7.3 が Task 4.4, 6.1, 3.6 にマッピング

5. E2E テストに関しては、design.md (line 278) で「E2E Required = No」と明記。理由: 内部リファクタリングであり、既存の E2E テストが間接的に検証する

**Reason**: Remote UI 対応は tasks.md で十分にカバーされており、追加の明記は不要。

---

## Response to Info (Low Priority)

| #    | Issue     | Judgment      | Reason         |
| ---- | --------- | ------------- | -------------- |
| S-1 | デバッグログ追加推奨 | No Fix Needed | 実装時の詳細事項。Task 1.1 で実装者がsteering/logging.mdに従って判断 |
| S-2 | LLMEngineRegistry統合への設計考慮 | No Fix Needed | Out of Scopeとして requirements.md に明記済み。「次のフェーズで検討」 |
| S-3 | 未知engineIdの警告ログ | No Fix Needed | design.md で「engineIdをそのままコマンドとして使用」と定義。実装の詳細 |

---

## Files to Modify

| File   | Changes   |
| ------ | --------- |
| なし | 修正不要 |

---

## Conclusion

全2件の Warning と 3件の Info/Suggestion を評価した結果、いずれも **No Fix Needed** と判断しました。

**理由の要約**:
- W-1: テスト戦略は design.md で十分に定義されている。IPCハンドラーは薄いラッパーであり、サービス層のテストで十分なカバレッジを確保
- W-2: Remote UI 対応は Task 4.4, 6.1 で明確にカバーされている
- Info 項目: いずれも実装時の詳細事項であり、仕様書への追加は不要

**次のステップ**:
実装フェーズに進むことを推奨します。`/kiro:spec-impl unified-engine-command-resolution` を実行してください。

---

_This reply was generated by the document-review-reply command._
