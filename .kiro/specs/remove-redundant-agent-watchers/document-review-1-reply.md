# Response to Document Review #1

**Feature**: remove-redundant-agent-watchers
**Review Date**: 2026-01-31
**Reply Date**: 2026-01-31

---

## Response Summary

| Severity | Issues | Fix Required | No Fix Needed | Needs Discussion |
| -------- | ------ | ------------ | ------------- | ---------------- |
| Critical | 0      | 0            | 0             | 0                |
| Warning  | 2      | 1            | 1             | 0                |
| Info     | 3      | 0            | 3             | 0                |

---

## Response to Critical Issues

なし

---

## Response to Warnings

### W1: E2E テスト名の不明確さ

**Issue**: Tasks 7.3 に具体的な E2E テスト名（例: `agent-badge.e2e.ts`）を追記するか、または「既存の E2E テストスイート全体を実行」と明記

**Judgment**: **Fix Required** ✅

**Evidence**:
E2E テストファイルを調査した結果、Agent バッジ関連のテストは以下のファイルで実行されている：
- `e2e-wdio/project-agent-startup.e2e.spec.ts` - Agent の起動とカウント検証
- `e2e-wdio/worktree-execution.e2e.spec.ts` - Worktree バッジ表示テスト

ただし、「SpecList の Agent 数バッジ」に特化した E2E テストは明示的に存在しない。本変更は削除のみであり、既存の E2E テストスイート全体が通過すれば機能維持が確認できる。

**Action Items**:
- tasks.md の Task 7.3 に「`task electron:test:e2e` で E2E テストスイート全体が通過することを確認」と明記する
- Agent バッジ専用の E2E テストは存在しないことを明確にし、既存テストで検証する方針を記載

---

### W2: Remote UI 呼び出し箇所の確認

**Issue**: Remote UI が WebSocketApiClient 経由で `switchAgentWatchScope` を呼び出している場合、削除後にエラーが発生する可能性がある。

**Judgment**: **No Fix Needed** ❌

**Evidence**:
実際のコードを確認した結果、Remote UI からの `switchAgentWatchScope` 呼び出しは以下の1箇所のみ：

```typescript
// src/shared/stores/bugStore.ts:183
await apiClient.switchAgentWatchScope(`bug:${bugId}`);
```

この呼び出しは `bugStore.selectBug()` メソッド内にあり、Tasks 3.2 で「`selectBug()` 内の `apiClient.switchAgentWatchScope()` 呼び出しを削除」として明確にカバーされている。

また、Design の「Interface Changes & Impact Analysis」セクションにも以下の対応が記載済み：

| Caller | Callee | 対応 |
|--------|--------|------|
| bugStore.selectBug() | apiClient.switchAgentWatchScope | 呼び出し行削除 |

grep 検索結果でも、本番コードでの `switchAgentWatchScope` 呼び出しは全て Tasks でカバーされていることを確認。Task 6.1 の grep 検証で漏れがないことも二重チェックされる。

**Action Items**: なし（既存の Tasks でカバー済み）

---

## Response to Info (Low Priority)

| #    | Issue     | Judgment      | Reason         |
| ---- | --------- | ------------- | -------------- |
| I1 | テストファイル存在確認 | No Fix Needed | 実装時に Design 記載のテストファイルが存在することを確認すればよい。ドキュメント修正は不要 |
| I2 | API ドキュメント更新 | No Fix Needed | internal API であり、外部ドキュメントは存在しない |
| I3 | Decision Log が優れている | No Fix Needed | 肯定的なフィードバック。対応不要 |

---

## Files to Modify

| File   | Changes   |
| ------ | --------- |
| `.kiro/specs/remove-redundant-agent-watchers/tasks.md` | Task 7.3 に E2E テストスイート全体実行を明記 |

---

## Conclusion

2件の Warning のうち、**1件のみ修正が必要**（W1: E2E テスト名の明記）。

W2（Remote UI 呼び出し箇所）は既存の Tasks で適切にカバーされており、追加の修正は不要。

修正内容は tasks.md への軽微な文言追加のみであり、アーキテクチャや実装方針に影響はない。

---

## Applied Fixes

**Applied Date**: 2026-01-31
**Applied By**: --autofix

### Summary

| File | Changes Applied |
| ---- | --------------- |
| `.kiro/specs/remove-redundant-agent-watchers/tasks.md` | Task 7.3 に E2E テストスイート全体実行の明記と Note 追加 |

### Details

#### .kiro/specs/remove-redundant-agent-watchers/tasks.md

**Issue(s) Addressed**: W1

**Changes**:
- Task 7.3 の記述を明確化
- E2E テストスイート全体の実行を優先する記述に変更
- Agent バッジ専用 E2E テストが存在しないことの Note を追加
- `project-agent-startup.e2e.spec.ts` を参照先として明記

**Diff Summary**:
```diff
- [ ] 7.3 E2E テストで Agent 関連機能が動作することを確認する
-   - Agent 開始時に SpecList バッジが更新されることを確認
-   - Agent 停止時に SpecList バッジが更新されることを確認
-   - `task electron:test:e2e` で全テストが通過することを確認
+ [ ] 7.3 E2E テストで Agent 関連機能が動作することを確認する
+   - `task electron:test:e2e` で E2E テストスイート全体が通過することを確認
+   - Agent 開始時に SpecList バッジが更新されることを確認（手動確認 or 既存 E2E でカバー）
+   - Agent 停止時に SpecList バッジが更新されることを確認（手動確認 or 既存 E2E でカバー）
+   - Note: Agent バッジ専用の E2E テストは存在しないが、`project-agent-startup.e2e.spec.ts` 等の既存テストで Agent ライフサイクルが検証される
```

---

_Fixes applied by document-review-reply command._

---

_This reply was generated by the document-review-reply command._
