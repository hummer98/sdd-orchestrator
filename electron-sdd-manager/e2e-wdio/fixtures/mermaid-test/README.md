# Mermaid Test Fixture

This fixture is used for E2E testing of Mermaid diagram rendering in ArtifactEditor.

## Purpose

- Test Mermaid diagram rendering in preview mode
- Test error handling for invalid Mermaid syntax
- Test that non-Mermaid code blocks are not affected
