# Response to Document Review #1

**Feature**: schedule-task-scheduler-activation
**Review Date**: 2026-01-31
**Reply Date**: 2026-01-31

---

## Response Summary

| Severity | Issues | Fix Required | No Fix Needed | Needs Discussion |
| -------- | ------ | ------------ | ------------- | ---------------- |
| Critical | 0      | 0            | 0             | 0                |
| Warning  | 3      | 1            | 2             | 0                |
| Info     | 2      | 0            | 2             | 0                |

---

## Response to Warnings

### W1: Criterion 2.2の統合テスト不在

**Issue**: Criterion 2.2「Rendererアクティビティ報告でlastActivityTime更新」がTask 4（統合テスト）で明示的にテストされていない。

**Judgment**: **No Fix Needed** ❌

**Evidence**:

1. **Design 134行目**: 「2.2 | Rendererアクティビティ報告でlastActivityTime更新 | idleTimeTracker.setLastActivityTime, useIdleTimeSync | **既存: 実装済み**」
2. **Specの目的**: 本Specは「schedule-task-execution Specの実装漏れを完成させる」ことが目的（Requirements Introduction参照）
3. **既存実装の確認**: `idleTimeTracker.setLastActivityTime()`と`useIdleTimeSync`フックは既に実装済みであり、本Specで新規実装・修正する対象ではない
4. **設計意図**: Requirements Traceability表で「既存: 実装済み」と明示されている機能は、本Specの検証対象外

既存実装の動作確認は、実装時に手動または既存の`idleTimeTracker.test.ts`等で確認可能であり、本Specの統合テストに含める必要はない。

---

### W2: Remote UI対応の明示的記載なし

**Issue**: tech.mdの「新規Spec作成時の確認事項」でRemote UI対応の明記が求められているが、requirements.mdまたはdesign.mdに明示的に記載されていない。

**Judgment**: **Fix Required** ✅

**Evidence**:

1. **tech.md 218行目**: 「`requirements.md` に「Remote UI対応: 要/不要」を記載」と明記
2. **現状**: requirements.mdの「Out of Scope」セクションでは「UIの変更（既存UIはそのまま使用）」としか記載されていない
3. **解釈**: 本SpecはMain Process側のスケジューラー統合であり、Remote UIへの影響はないと推測されるが、steeringの要求に従い明示的に記載すべき

**Action Items**:

- requirements.mdの「Out of Scope」セクションに「Remote UI対応: 不要（Main Process内部の実装のみで、UIレイヤーへの変更なし）」を追記

---

### W3: Task 5.1の完了基準不明確

**Issue**: Task 5.1「既存ユニットテストの確認」の完了基準が不明確。「必要に応じてモックを更新」の判断基準がない。

**Judgment**: **No Fix Needed** ❌

**Evidence**:

1. **tasks.md Task 5.1の記載内容**:
   - 「既存のscheduleTaskCoordinator.test.tsを確認」
   - 「依存関係注入の変更による影響がないことを確認」
   - 「必要に応じてモックを更新」

2. **暗黙の完了基準**: テストタスクの完了基準は「テストが全て通過すること」が業界標準。Task 5.1の目的は「既存テストが壊れていないことの確認」であり、以下のシンプルな基準で判断可能:
   - テストが通過する → モック更新不要 → 完了
   - テストが失敗する → モック更新必要 → 更新後に再実行 → 通過 → 完了

3. **過剰な明示化は不要**: 実装者にとって「テストが通過すれば完了」は自明であり、これを明文化することは冗長。tasks.mdの簡潔さを維持すべき。

---

## Response to Info (Low Priority)

| #   | Issue                 | Judgment      | Reason                                                                    |
| --- | --------------------- | ------------- | ------------------------------------------------------------------------- |
| S1  | ロギング実装詳細      | No Fix Needed | steering/logging.mdは実装時の参照ドキュメントであり、Specへの記載は不要   |
| S2  | E2E検証               | No Fix Needed | Out of ScopeでE2Eテスト除外を明記済み。手動検証は実装者の裁量で実施可能 |

---

## Files to Modify

| File             | Changes                                                                                    |
| ---------------- | ------------------------------------------------------------------------------------------ |
| requirements.md  | 「Out of Scope」セクションに「Remote UI対応: 不要」を追記                                  |

---

## Conclusion

**判断結果**: 3件の警告のうち、1件（W2: Remote UI対応の明示）のみ修正が必要。他の2件は既存設計の意図に沿っており、修正不要と判断。

**次のステップ**: `--autofix`フラグにより、W2の修正を自動適用する。

---

## Applied Fixes

**Applied Date**: 2026-01-31
**Applied By**: --autofix

### Summary

| File            | Changes Applied                                            |
| --------------- | ---------------------------------------------------------- |
| requirements.md | 「Out of Scope」セクションに「Remote UI対応: 不要」を追記 |

### Details

#### requirements.md

**Issue(s) Addressed**: W2

**Changes**:
- 「Out of Scope」セクションに「Remote UI対応: 不要（Main Process内部の実装のみで、UIレイヤーへの変更なし）」を追記

**Diff Summary**:
```diff
 ## Out of Scope

 - UIの変更（既存UIはそのまま使用）
 - スケジュールタスクの新規機能追加
 - 既存の `schedule-task-execution` Specのドキュメント修正
 - E2Eテスト（ユニット/統合テストのみ）
+- Remote UI対応: 不要（Main Process内部の実装のみで、UIレイヤーへの変更なし）
```

---

_Fixes applied by document-review-reply command._

---

_This reply was generated by the document-review-reply command._
